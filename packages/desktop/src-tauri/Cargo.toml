[package]
name = "shard-den-desktop"
version.workspace = true
edition.workspace = true

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[workspace]

[package.metadata.tauri]
config = "tauri.conf.json"
